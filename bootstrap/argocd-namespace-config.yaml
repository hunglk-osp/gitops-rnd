apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-3"  # Tạo trước tất cả
data:
  # Cho phép ArgoCD watch Application CRDs trong tất cả namespace
  application.namespaces: "*"
